// src/components/StarfieldBackground.tsx
import { FC } from 'react';

const StarfieldBackground: FC = () => {
  // This component has been simplified to use CSS for the background
  // to resolve compilation errors and improve performance.
  // The actual effect is now handled by the `body::before` and `body::after`
  // pseudo-elements defined in src/index.css.
  return null;
};

export default StarfieldBackground;